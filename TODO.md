# Task: Rename Group type fields

- [x] Read all relevant files (lib/data.tsx, app/[id]/page.tsx, app/groups/page.tsx, components/GroupCard.tsx, components/TaskItem.tsx)
- [x] Update `lib/data.tsx`: rename `id` → `groupId`, `name` → `groupName`, `subject` → `subjectName` in type and mock data
- [x] Update `app/[id]/page.tsx` to use new field names
- [x] Update `app/groups/page.tsx` to use new field names
- [x] Update `components/GroupCard.tsx` to use new field names
- [x] Update mock data values: groupName, subjectName for group 1; groupId for groups 1 and 2
